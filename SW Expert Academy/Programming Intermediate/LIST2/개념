1. 이차원리스트
1) 이차원 리스트 입력받기
   mylist=[0]*n
   for i in range(n):
      mylist[i]=list(map(int,input().split()))
2) 이차원리스트에서 1이 들어간 곳 찾기
   newlist=[(i,j) for i in range(n) for j in range(m) if mylist[i][j]==1]
3) 이차원리스트 순회
  (1)행 우선 순회
  (2)열 우선 순회
  (3)지그재그 순회
4) zip(iterable*)
  - 동일한 갯수로 이루어진 리스트를 묶어주는 역할

2. 부분집합
1) Loop를 이용하여 확인하고 부분집합을 생성한다.
2) 비트연산자 (&,|,<<,>>)를 이용하여 부분집합을 생성한다.

3. 검색
1) 순차 검색
  (1)정렬되지 않은 자료
    - 앞에서부터 순서대로 키값과 비교하여 찾아낸다.
  (2)정렬되어있는 자료
    - 앞에서부터 순서대로 하지만, 키값보다 클 경우 검색을 멈춘다.
2) 이진 검색 : 항상 자료가 정렬되어있어야한다.
  - 자료의 중앙값을 찾고 키값과 비교하여 키값이 더 작으면 왼쪽의 반, 크면 오른쪽의 반을 지정하여 반복 검색한다.
3) 인덱스

4. 정렬
1) 셀렉션 알고리즘 : k번째로 작은 원소를 찾는 알고리즘
  - 정렬한 다음(작은원소를 찾아 리스트의 앞쪽으로 이동) k번째 인덱스의 원소를 가져온다.
2) 선택정렬 : 셀렉션 알고리즘을 전체에 적용한 것
  - 가장 작은 원소를 찾아 맨 앞 원소와 위치를 교환한다.
